# ODPT Station

## Setup

Register with ODPT, install Rhinestone and `requests`, and set
`ODPT_CONSUMER_KEY`. Run `python examples/12_odpt_station/example.py`.

The consumer key is resolved only when the request is opened. It is never put
in Config, Source metadata, or Provenance.
